name: Keep Railway Pipeline Warm

on:
  schedule:
    - cron: "*/10 * * * *"  # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping intake health
        run: curl -fsS https://pipeline-production-e5b8.up.railway.app/health || exit 0
